extends: users/admin
title: Editor
description: Can edit all content but cannot use administrative tools (e.g., user management).

permissions:
  access:
    system: false
    languages: false
    backups: false
    users: false

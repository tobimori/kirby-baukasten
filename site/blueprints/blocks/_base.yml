preview: fields
wysiwyg: true

tabs:
  content:
    label: tab.content.label
    fields:
  advanced:
    label: tab.advanced.label
    fields:
      blockId:
        label: field.blockId.label
        type: slug
        before: "#"
        icon: false

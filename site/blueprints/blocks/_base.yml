preview: fields
wysiwyg: true

tabs:
  content:
    label: Content
    fields:
  advanced:
    label: Advanced
    fields:
      blockId:
        label: Unique ID (Anchor)
        type: slug
        before: "#"
        icon: false

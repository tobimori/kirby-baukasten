icon: bolt
label: admin-tools
columns:
  - width: 1/4
    fields:
      _caching:
        label: caching
        type: headline
  - width: 3/4
    fields:
      flushCache:
        label: "{{ t('flush-cache') }}"
        type: janitor
        icon: trash
        command: 'janitor:flush'
  - width: 1
    fields:
      _admin:
        type: line
  - width: 1/4
    fields:
      _other:
        label: other
        type: headline
  - width: 3/4
    fields:
      downloadContent:
        label: "{{ t('download-content') }}"
        type: janitor
        icon: archive
        command: 'download-content'
